local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/Gamer-MouyuseV6/Xrypto-Lib/main/Source.lua"))()

local Config = Library:CreateConfig({
    Enable = true,
    Folder = "Hub",
    fileName = "Config"
})

local Key = Library:CreateLoader({
    Enable = true,                   -- Enables the loader
    Tittle = "Frog Hub",             -- Title of the loader (likely displayed in the UI)
    Tittle2 = "Key system",          -- A second Tittle below Tittle
    Key = {"Key"},                   -- The valid key(s) for authentication (in this case, a single key: "Key")
    SaveKey = true,                  -- Saves the key (e.g., to a file or storage) for future use
    AutoCheckIfKeyiscorrect = true,  -- Automatically checks the keybox if it has the right key 
    ToggleKeybind = "RightControl",  -- Keybind to toggle the loader UI (Right Ctrl key)
    SecondAction = {                 -- Configuration for a secondary action (e.g., a button or link)
        GetLinkType = "Link",        -- Specifies the type of secondary action (a link in this case)
        Link = "WebsiteOrDiscord",   -- Placeholder for a website or Discord link
        Button = GetLink,   
        KeyBox = KeyBox, 
        Callback = function()
            -- code here when the key is correct --
        end
    }
})
